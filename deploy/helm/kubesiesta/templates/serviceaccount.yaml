{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "kubesiesta.serviceAccountName" . }}
  labels:
    {{- include "kubesiesta.labels" . | nindent 4 }}
{{- end }}
